import { PackageCheck } from "lucide-react";
import { PlaceholderPage } from "@/components/placeholder-page";

export default function PacklistenPage() {
  return (
    <PlaceholderPage
      description="Packlisten werden später Materialanforderungen, Buchungen und Rückläufe für Jobs abbilden."
      icon={PackageCheck}
      title="Packlisten"
    />
  );
}
