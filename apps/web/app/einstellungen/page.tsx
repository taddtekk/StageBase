import { Settings } from "lucide-react";
import { PlaceholderPage } from "@/components/placeholder-page";

export default function EinstellungenPage() {
  return (
    <PlaceholderPage
      description="Einstellungen werden später Organisation, Standorte, Rollen und technische Konfigurationen aufnehmen."
      icon={Settings}
      title="Einstellungen"
    />
  );
}
